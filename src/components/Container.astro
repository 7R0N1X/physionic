---
const {class: className, title, description, id} = Astro.props
---

<section class={`${className} w-full py-20 max-sm:px-4`} id={id}>
  <h2 class="text-4xl font-bold text-center mb-5">{title}</h2>
  <p class="sm:text-lg text-center text-[#737373] mb-14">{description}</p>
  <slot />
</section>